import { ItemDefinitions } from '../../../item-definitions';

import * as textureData from './texture';

export const itemId = ItemDefinitions.ItemId.Shirt;

export const itemType = ItemDefinitions.ItemType.Clothing;

export const texture = textureData;

export const equipSlots = [ItemDefinitions.ItemEquipSlot.Chest, ItemDefinitions.ItemEquipSlot.Arm, ItemDefinitions.ItemEquipSlot.Arm];
